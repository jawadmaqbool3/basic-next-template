import type { TCreateUserRequest } from "@/types/Requests/CreateUser";
export default function filterTypesKey(before: TCreateUserRequest) {
    
  //loop through before
//   function hasKey<T, K extends string>(obj: T, key: K): TCreateUserRequest<T, K> {
//     return key in obj as TCreateUserRequest<T, K>;
//   }
  

//   for (const [key, value] of Object.entries(before)) {
//     if (hasKey<TCreateUserRequest, key>) {
        
//     }
//   }
}
